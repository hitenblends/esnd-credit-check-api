# Shopify Custom App Credentials
# For custom apps created in store admin (no OAuth required)
SHOPIFY_ACCESS_TOKEN=YOUR_ACCESS_TOKEN_HERE

# OAuth Credentials (only needed if switching to public app later)
# SHOPIFY_API_KEY=your_api_key_here
# SHOPIFY_API_SECRET=your_client_secret_here
# SHOPIFY_PROXY_SECRET=your_client_secret_here
# SHOPIFY_REDIRECT_URI=https://esnd-credit-check-api.onrender.com/auth/callback

# Server Configuration
PORT=3000
NODE_ENV=production

# External Credit Check API
CREDIT_CHECK_API_URL=http://54.148.31.213/api/creditCheck/

# ========================================
# CUSTOM APP SETUP COMPLETE!
# ========================================
# This app uses a custom Shopify app (no OAuth required)
# The access token above provides direct Admin API access
#
# To use this setup:
# 1. Copy this file to .env: cp env.example .env
# 2. Replace YOUR_ACCESS_TOKEN_HERE with your actual access token
# 3. No additional setup required
#
# If you want to switch to a public app later:
# 1. Comment out SHOPIFY_ACCESS_TOKEN
# 2. Uncomment and configure OAuth credentials
# 3. Update server.js to use OAuth flow
# ========================================
